# URL of the deployed Django backend (no trailing slash)
# In development this is not needed — Vite proxies /api automatically
# In production (Vercel) set this to your Railway/Render backend URL
VITE_API_URL=https://your-backend.railway.app/api
